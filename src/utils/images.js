// Placeholder image color helper — used until real project photography is
// dropped into src/assets/images. Keeps thumbnails visually distinct per
// project without requiring binary assets in this scaffold.
export function getPlaceholderGradient(accentColor = "#FF4B4B") {
  return `linear-gradient(135deg, ${accentColor}22, ${accentColor}55)`;
}
